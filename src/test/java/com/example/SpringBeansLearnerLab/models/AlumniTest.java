package com.example.SpringBeansLearnerLab.models;

import com.example.SpringBeansLearnerLab.configurations.StudentConfig;
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.mockito.BDDMockito;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.Optional;

import static org.junit.jupiter.api.Assertions.*;

@RunWith(SpringRunner.class)
@SpringBootTest
class AlumniTest {
//    @Autowired
//    Alumni alumni;
//
//    @MockBean
//    StudentConfig studentConfig;

    @Test
    void executeBootcamp() {
//        Long givenId = 1L;
//        Students curr = new Students(new Student(givenId, "New Student!"));
//        BDDMockito
//                .given(studentConfig.previousStudents())
//                .willReturn(Optional.of(curr));


    }
}